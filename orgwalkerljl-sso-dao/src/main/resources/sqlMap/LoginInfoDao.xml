<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.walkerljl.sso.dao.LoginInfoDao">
	<!-- ======================= 以下sql需要根据表结构自定义 =========================================================================== -->
	<!-- 定义实体映射 -->
	<resultMap type="loginInfo" id="resultMap">
		<result property="id" column="id" />
		<result property="userId" column="user_id" />
		<result property="userName" column="user_name" />
		<result property="loginIp" column="login_ip" />
		<result property="loginDate" column="login_date" />
		<result property="logoutDate" column="logout_date" />
		<result property="loginAgent" column="login_agent" />
	</resultMap>

	<!-- 表名 -->
	<sql id="TABLE_NAME"><![CDATA[sso_login_info]]></sql>

	<!-- 插入字段 -->
	<sql id="INSERT_COLUMNS">
  		<![CDATA[(user_id,user_name,login_ip,login_date,logout_date,login_agent)]]>
	</sql>

	<!-- 插入字段值 -->
	<sql id="INSERT_VALUES">
		<![CDATA[(#{userId},#{userName},#{loginIp},#{loginDate},#{logoutDate},#{loginAgent})]]>
	</sql>

	<!-- 批量插入字段值 -->
	<sql id="BATCH_INSERT_VALUES">
		<![CDATA[(#{item.userId},#{item.userName},#{item.loginIp},#{item.loginDate},#{item.logoutDate},#{item.loginAgent})]]>
	</sql>

	<!-- 删除条件 -->
	<sql id="DELETE_BY_CONDITION_WHERE_CLAUSE">
		<where>
			<if test="id != null"><![CDATA[AND id = #{id}]]></if>
			<if test="userId != null and userId != ''"><![CDATA[AND user_id = #{userId}]]></if>
			<if test="userName != null and userName != ''"><![CDATA[AND user_name = #{userName}]]></if>
			<if test="loginIp != null and loginIp != ''"><![CDATA[AND login_ip = #{loginIp}]]></if>
			<if test="loginDate != null"><![CDATA[AND login_date = #{loginDate}]]></if>
			<if test="logoutDate != null"><![CDATA[AND logout_date = #{logoutDate}]]></if>
			<if test="loginAgent != null"><![CDATA[AND login_agent = #{loginAgent}]]></if>
		</where>
	</sql>

	<!-- 更新列字段,字段不为NULL则更新,除开主键 -->
	<sql id="UPDATE_COLUMN_SET">
		<set>
			<if test="userId != null and userId != ''"><![CDATA[a.user_id = #{userId},]]></if>
			<if test="userName != null and userName != ''"><![CDATA[a.user_name = #{userName},]]></if>
			<if test="loginIp != null and loginIp != ''"><![CDATA[a.login_ip = #{loginIp},]]></if>
			<if test="loginDate != null"><![CDATA[a.login_date = #{loginDate},]]></if>
			<if test="logoutDate != null"><![CDATA[a.logout_date = #{logoutDate},]]></if>
			<if test="loginAgent != null"><![CDATA[a.login_agent = #{loginAgent},]]></if>
		</set>
	</sql>

	<!-- 实体对应表中的所有字段 -->
	<sql id="QUERY_COLUMN_LIST">
		<![CDATA[a.id,a.user_id,a.user_name,a.login_ip,a.login_date,a.logout_date,a.login_agent]]>
	</sql>

	<!-- 全部查询条件 -->
	<sql id="QUERY_WHERE_CLAUSE">
		<where>
			<if test="id != null"><![CDATA[AND a.id = #{id}]]></if>
			<if test="userId != null and userId != ''"><![CDATA[AND a.user_id = #{userId}]]></if>
			<if test="userName != null and userName != ''"><![CDATA[AND a.user_name = #{userName}]]></if>
			<if test="loginIp != null and loginIp != ''"><![CDATA[AND a.login_ip = #{loginIp}]]></if>
			<if test="loginDate != null"><![CDATA[AND a.login_date = #{loginDate}]]></if>
			<if test="logoutDate != null"><![CDATA[AND a.logout_date = #{lologoutDateginDate}]]></if>
			<if test="loginAgent != null"><![CDATA[AND a.login_agent = #{loginAgent}]]></if>
		</where>
	</sql>
	<!-- ======================= 自定义SQL =========================================================================== -->
	
	<!-- ======================= 以下sql只需要修改 parameterType值 =========================================================================== -->
	<!-- 排序 -->
	<sql id="QUERY_ORDER_CONDITION">
  		<![CDATA[order by a.id desc]]>
	</sql>

	<!-- 分页 -->
	<sql id="QUERY_LIMIT_CONDITION">
		<if
			test="startIndex !=null and startIndex &gt;= 0 and pageSize !=null and pageSize &gt; 0">
  			<![CDATA[LIMIT #{startIndex},#{pageSize}]]>
		</if>
	</sql>

	<!-- 主键列表WHERE语句 -->
	<sql id="KEYS_FOREACH_CLAUSE">
		<foreach collection="list" open="(" separator="," close=")"
			item="item" index="index">
			<![CDATA[#{item}]]>
		</foreach>
	</sql>

	<!-- 添加,自动填充主键字段值 -->
	<insert id="insert" parameterType="loginInfo" useGeneratedKeys="true"
		keyProperty="id">
		<![CDATA[INSERT INTO]]>
		<include refid="TABLE_NAME" />
		<include refid="INSERT_COLUMNS" />
		<![CDATA[VALUES]]>
		<include refid="INSERT_VALUES" />
	</insert>

	<!-- 批量添加 -->
	<insert id="batchInsert" parameterType="java.util.List">
		<![CDATA[INSERT INTO]]>
		<include refid="TABLE_NAME" />
		<include refid="INSERT_COLUMNS" />
		<![CDATA[VALUES]]>
		<foreach collection="list" separator="," item="item" index="index">
			<include refid="BATCH_INSERT_VALUES" />
		</foreach>
	</insert>

	<!-- 删除,主键列表 -->
	<delete id="deleteByKeys" parameterType="java.util.List">
		<![CDATA[DELETE FROM]]>
		<include refid="TABLE_NAME" /> 
		<![CDATA[WHERE id IN]]>
		<include refid="KEYS_FOREACH_CLAUSE" />
	</delete>

	<!-- 删除,字段不为NULL则为删除条件 -->
	<delete id="deleteByCondition" parameterType="loginInfo">
		<![CDATA[DELETE FROM]]>
		<include refid="TABLE_NAME" />
		<include refid="DELETE_BY_CONDITION_WHERE_CLAUSE" />
	</delete>

	<!-- 更新状态,主键列表 -->
	<update id="updateStatusByKeys" parameterType="java.util.Map">
		<![CDATA[UPDATE]]>
		<include refid="TABLE_NAME" />
		<![CDATA[a]]>
		<include refid="UPDATE_COLUMN_SET" /> 
		<![CDATA[WHERE a.id IN]]>
		<include refid="KEYS_FOREACH_CLAUSE" />
	</update>

	<!-- 更新,主键 -->
	<update id="updateByKey" parameterType="loginInfo">
		<![CDATA[UPDATE]]>
		<include refid="TABLE_NAME" />
		<![CDATA[a]]>
		<include refid="UPDATE_COLUMN_SET" />
		<![CDATA[WHERE a.id = #{id}]]>
	</update>

	<!-- 查询,主键 -->
	<select id="selectByKey" parameterType="long" resultMap="resultMap">
		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST" />
    	<![CDATA[FROM]]>
		<include refid="TABLE_NAME" />
  		<![CDATA[a WHERE a.id = #{id}]]>
	</select>

	<!-- 查询,主键列表 -->
	<select id="selectListByKeys" parameterType="java.util.List"
		resultMap="resultMap">
  		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST" />
    	<![CDATA[FROM]]>
		<include refid="TABLE_NAME" />
    	<![CDATA[a WHERE a.id IN]]>
		<include refid="KEYS_FOREACH_CLAUSE" />
	</select>

	<!-- 查询,字段不为NULL则为条件 -->
	<select id="selectList" parameterType="loginInfo" resultMap="resultMap">
		<![CDATA[SELECT]]>
		<include refid="QUERY_COLUMN_LIST" />
    	<![CDATA[FROM]]>
		<include refid="TABLE_NAME" />
		<![CDATA[a]]>
		<include refid="QUERY_WHERE_CLAUSE" />
		<include refid="QUERY_ORDER_CONDITION" />
		<include refid="QUERY_LIMIT_CONDITION" />
	</select>

	<!-- 总数查询,字段不为NULL则为条件 -->
	<select id="selectListCount" parameterType="loginInfo" resultType="int">
  		<![CDATA[SELECT COUNT(1) FROM]]>
		<include refid="TABLE_NAME" />
  		<![CDATA[a]]>
		<include refid="QUERY_WHERE_CLAUSE" />
	</select>
</mapper>